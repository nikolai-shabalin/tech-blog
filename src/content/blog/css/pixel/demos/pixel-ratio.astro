<figure class="css-pixel-demo">
  <div class="css-pixel-demo__wrapper">

    <div class="css-pixel-demo__item">
      <div class="css-pixel-demo__pixel css-pixel-demo__pixel--1x"></div>
      <figcaption>
        1 CSS px = 1 физический px
      </figcaption>
    </div>

    <div class="css-pixel-demo__item">
      <div class="css-pixel-demo__pixel css-pixel-demo__pixel--2x"></div>
      <figcaption>
        1 CSS px = 2×2 физических px
      </figcaption>
    </div>

  </div>
</figure>

<style>
  .css-pixel-demo {
    margin: 32px 0;
  }

  .css-pixel-demo__wrapper {
    display: flex;
    gap: 32px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .css-pixel-demo__item {
    text-align: center;
  }

  .css-pixel-demo__pixel {
    width: 120px;
    height: 120px;
    border: 2px solid #222;
    border-radius: 8px;
    margin-bottom: 12px;
    background-color: #fff;
  }

  /* обычный экран — один пиксель */
  .css-pixel-demo__pixel--1x {
    background-image:
      linear-gradient(to right, rgb(59 130 246 / 0.7) 1px, transparent 1px),
      linear-gradient(to bottom, rgb(59 130 246 / 0.7) 1px, transparent 1px);
    background-size: 120px 120px; /* одна большая "клетка" */
  }

  /* retina — 2×2 */
  .css-pixel-demo__pixel--2x {
    background-image:
      linear-gradient(to right, rgb(59 130 246 / 0.7) 1px, transparent 1px),
      linear-gradient(to bottom, rgb(59 130 246 / 0.7) 1px, transparent 1px);
    background-size: 60px 60px; /* делим на 2 по каждой оси */
  }

  .css-pixel-demo figcaption {
    font-size: 14px;
    color: #555;
    line-height: 1.4;
  }

  @media (prefers-color-scheme: dark) {
    .css-pixel-demo__pixel {
      border-color: #e5e7eb;
      background-color: #09090b;
    }

    .css-pixel-demo__pixel--1x,
    .css-pixel-demo__pixel--2x {
      background-image:
        linear-gradient(to right, rgb(96 165 250 / 0.85) 1px, transparent 1px),
        linear-gradient(to bottom, rgb(96 165 250 / 0.85) 1px, transparent 1px);
    }

    .css-pixel-demo figcaption {
      color: #d4d4d8;
    }
  }
</style>
